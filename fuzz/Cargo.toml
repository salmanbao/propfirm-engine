[package]
name = "fuzz"
version = "0.1.0"
edition = "2021"

[dependencies]
libfuzzer-sys = "0.4"
propfirm-engine = { path = "..", default-features = false, features = ["serialization", "in-memory-store"] }

[workspace]
